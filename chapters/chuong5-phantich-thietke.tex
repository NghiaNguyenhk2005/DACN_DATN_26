\chapter{Phân tích và thiết kế}
\label{chap:phantich}

Chương này mô hình hóa các quy trình và tương tác của hệ thống (mục~\ref{sec:phantich}), lựa chọn giải pháp công nghệ (mục~\ref{sec:giaiphapcongnghe}) và trình bày thiết kế chi tiết từ kiến trúc, cơ sở dữ liệu, mô hình AI tới API, giao diện, test case và chính sách xử lý với bên thứ ba (mục~\ref{sec:thietke}), dựa trên các yêu cầu đã xác lập tại Chương~\ref{chap:hethong}.

\section{Phân tích}
\label{sec:phantich}

\subsection{Mô hình hóa quy trình nghiệp vụ}
\label{subsec:mohinhquytrinh}

Tám quy trình nghiệp vụ mô tả tại mục~\ref{sec:quytrinhnghiepvu} được mô hình hóa bằng lưu đồ. Các ký hiệu dùng chung cho toàn bộ lưu đồ được giải thích tại Hình~\ref{fig:chu-thich-flowchart}.

\input{flowcharts/00-chu-thich}

\clearpage
\subsubsection*{Đăng ký và xác thực nhà cung cấp, kiểm duyệt chứng nhận}
Hình~\ref{fig:flow-dang-ky} minh họa quy trình trên dưới dạng lưu đồ, gồm các điểm kiểm tra OTP, eKYC và kiểm duyệt chứng nhận.
\input{flowcharts/01-dang-ky}

\clearpage
\subsubsection*{Đăng và kiểm duyệt sản phẩm, sinh mã QR truy xuất}
Hình~\ref{fig:flow-san-pham} minh họa quy trình đăng và kiểm duyệt sản phẩm, bao gồm nhánh hậu kiểm nhanh dành cho nhà cung cấp có uy tín cao.
\input{flowcharts/02-san-pham}

\clearpage
\subsubsection*{Thu mua vào kho của nền tảng}
Hình~\ref{fig:flow-thu-mua} minh họa quy trình thu mua --- bước đứng trước kênh bán lẻ và là điểm khác biệt căn bản so với một sàn thuần trung gian.
\input{flowcharts/08-thu-mua}

\clearpage
\subsubsection*{Quy trình bán lẻ}
Quy trình bán lẻ được minh họa qua hai lưu đồ nối tiếp: Hình~\ref{fig:flow-ban-le-a} (đặt hàng, thanh toán và xác nhận đơn) và Hình~\ref{fig:flow-ban-le-b} (giao hàng, nhận hàng và hoàn tất đơn).
\input{flowcharts/03-ban-le}
\input{flowcharts/03b-ban-le-giao-nhan}

\clearpage
\subsubsection*{Đánh giá sản phẩm và nhà cung cấp}
Hình~\ref{fig:flow-danh-gia} minh họa quy trình đánh giá, từ kiểm duyệt đánh giá tới cập nhật điểm uy tín và quyền lợi của nhà cung cấp.
\input{flowcharts/07-danh-gia}

\clearpage
\subsubsection*{Quy trình bán sỉ (B2B)}
Quy trình bán sỉ được minh họa qua hai lưu đồ nối tiếp: Hình~\ref{fig:flow-ban-si-a} (từ công bố bảng giá tới ký hợp đồng) và Hình~\ref{fig:flow-ban-si-b} (tạm giữ tiền, giao hàng và thanh toán theo từng đợt).
\input{flowcharts/04-ban-si}
\input{flowcharts/04b-ban-si-giao-hang}

\clearpage
\subsubsection*{Quản lý tồn kho, vận chuyển hàng dễ hư hỏng và xử lý khiếu nại}
Hình~\ref{fig:flow-van-chuyen} minh họa quy trình vận chuyển hàng dễ hư hỏng và xử lý khiếu nại.
\input{flowcharts/05-van-chuyen}

\clearpage
\subsubsection*{Quản trị hệ thống}
Hình~\ref{fig:flow-quan-tri} minh họa quy trình giám sát, xử lý vi phạm phân cấp và báo cáo của quản trị viên.
\input{flowcharts/06-quan-tri}

\clearpage
\subsection{Mô hình hóa tương tác}
\label{sec:sddchitiet}

Mục này trình bày sơ đồ use case tổng quát và theo từng tác nhân, cùng bảng đặc tả cho các use case chính, bám theo mã FR tại mục~\ref{sec:yeucauchucnang} và các quy trình nghiệp vụ tại mục~\ref{sec:quytrinhnghiepvu}.

\subsubsection*{Danh sách tác nhân (actor)}

Bảng~\ref{tab:actor-usecase} liệt kê các tác nhân, khớp với vai trò RBAC đã xác lập tại mục~\ref{sec:doituongnguoidung}.

\begingroup
\small
\renewcommand{\arraystretch}{1.25}
\begin{longtable}{L{4.0cm} L{3.0cm} L{6.5cm}}
\caption{Danh sách tác nhân trong sơ đồ use case}
\label{tab:actor-usecase} \\
\toprule
\rowcolor{tblheadbg}
\textbf{Tác nhân} & \textbf{Vai trò RBAC} & \textbf{Ghi chú} \\
\midrule
\endfirsthead
\toprule
\rowcolor{tblheadbg}
\textbf{Tác nhân} & \textbf{Vai trò RBAC} & \textbf{Ghi chú} \\
\midrule
\endhead
\bottomrule
\endfoot
Nhà cung cấp (nông dân/HTX) & \texttt{seller} & Tham gia cả hai luồng bán sỉ và bán lẻ (cung cấp hàng cho thu mua) \\
\rowcolor{tblaltbg}
Khách lẻ & \texttt{buyer\_retail} & Chỉ tham gia luồng bán lẻ (1P) \\
Khách sỉ (doanh nghiệp thu mua) & \texttt{buyer\_wholesale} & Chỉ tham gia luồng bán sỉ (3P) \\
\rowcolor{tblaltbg}
Bộ phận thu mua & \texttt{ops\_sourcing} & Vận hành nội bộ, riêng cho luồng bán lẻ (1P) --- ra quyết định thu mua theo lô \\
Quản trị viên kiểm duyệt & \texttt{admin\_moderator} & Kiểm duyệt hồ sơ, sản phẩm, chứng nhận \\
\rowcolor{tblaltbg}
Quản trị viên vận hành & \texttt{admin\_ops} & Xử lý vi phạm, khiếu nại, báo cáo thống kê, giám sát tồn kho/vận chuyển \\
Quản trị viên cấp cao & \texttt{admin} & Gộp toàn bộ quyền của \texttt{admin\_moderator} và \texttt{admin\_ops} (mục~\ref{sec:doituongnguoidung}, mục e3) \\
\rowcolor{tblaltbg}
Hệ thống AI \textit{(actor phụ)} & --- & Hỗ trợ gợi ý sản phẩm (FR12.1), dự báo (FR12.2), luật ngưỡng giám sát bất thường \\
Cổng thanh toán trung gian \textit{(actor phụ)} & --- & Bên ngoài hệ thống, xử lý thanh toán và ký quỹ (FR7) \\
\rowcolor{tblaltbg}
Đơn vị vận chuyển 3PL \textit{(actor phụ)} & --- & Bên ngoài hệ thống (FR8.1) \\
\end{longtable}
\endgroup

\noindent\textbf{Quan hệ generalization:} \texttt{admin} kế thừa toàn bộ use case của \texttt{admin\_moderator} và \texttt{admin\_ops} theo nguyên tắc đặc quyền tối thiểu đã xác lập tại mục~\ref{sec:doituongnguoidung} (NFR3.1).

\subsubsection*{Sơ đồ use case tổng quan}

Hình~\ref{fig:usecase-overview} trình bày sơ đồ tổng quan, nhóm theo năm gói chức năng ứng với các nhóm FR đã xác lập tại mục~\ref{sec:yeucauchucnang}.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{image/comparison/usecase-overview.png}
\caption{Sơ đồ Use Case tổng quan hệ thống Farmery}
\label{fig:usecase-overview}
\end{figure}

\subsubsection*{Sơ đồ use case chi tiết theo tác nhân}

Hình~\ref{fig:usecase-nhaban} minh họa chi tiết các use case của Nhà cung cấp quanh nhóm sản phẩm và truy xuất nguồn gốc (FR4), gồm đầy đủ quan hệ \texttt{<<include>>} (bắt buộc) và \texttt{<<extend>>} (mở rộng có điều kiện) theo chuẩn UML.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{image/comparison/usecase-nha-ban.png}
\caption{Sơ đồ Use Case chi tiết --- tác nhân Nhà cung cấp}
\label{fig:usecase-nhaban}
\end{figure}

Hình~\ref{fig:usecase-khachle} minh họa hành trình mua hàng bán lẻ đầy đủ của Khách lẻ (FR5).

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{image/comparison/usecase-nguoi-mua-le.png}
\caption{Sơ đồ Use Case chi tiết --- tác nhân Khách lẻ}
\label{fig:usecase-khachle}
\end{figure}

Hình~\ref{fig:usecase-khachsi} minh họa luồng yêu cầu báo giá, hợp đồng điện tử, ký quỹ và giao hàng theo đợt của Khách sỉ (FR6, FR7).

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{image/comparison/usecase-nguoi-mua-si.png}
\caption{Sơ đồ Use Case chi tiết --- tác nhân Khách sỉ}
\label{fig:usecase-khachsi}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{image/comparison/usecase-quan-tri-vien.png}
\caption{Sơ đồ Use Case chi tiết --- tác nhân Quản trị viên}
\label{fig:usecase-quantri}
\end{figure}

\subsubsection*{Bảng đặc tả các use case chính}

Các use case dưới đây được chọn đại diện cho mọi loại actor và loại luồng: thao tác đơn giản (UC-B2), luồng có sinh dữ liệu tự động (UC-B3), luồng có actor phụ bên ngoài (UC-C4, UC-D7), luồng nhiều bước có điều kiện rẽ nhánh (UC-D4), và luồng có AI hỗ trợ (UC-C7).

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-B2 --- Ghi nhật ký canh tác theo lô}
\label{tab:uc-b2} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-B2: Ghi nhật ký canh tác theo lô}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Nhà cung cấp (\texttt{seller}) \\
\rowcolor{tblaltbg}
\textbf{Tiền điều kiện} & Nhà cung cấp đã xác thực (trạng thái ``Đã xác thực''); đã tạo sản phẩm/lô hàng tương ứng (UC-B1) \\
\textbf{Luồng chính} &
1. Nhà cung cấp chọn lô hàng cần ghi nhật ký.\newline
2. Nhập giai đoạn canh tác (gieo trồng/chăm sóc/thu hoạch), ngày tháng, mô tả hoạt động.\newline
3. Tải ảnh minh chứng (nếu có).\newline
4. Hệ thống lưu bản ghi, gắn với lô tương ứng, đánh dấu thời điểm tạo (bất biến, không cho sửa trực tiếp --- NFR9.1).\newline
5. Nếu đủ điều kiện tối thiểu ($\geq$1 bản ghi + ảnh sản phẩm thật), chuyển sản phẩm sang trạng thái đủ điều kiện xét duyệt công khai (UC-B4). \\
\rowcolor{tblaltbg}
\textbf{Luồng thay thế/ngoại lệ} &
4a. Cần đính chính bản ghi cũ: hệ thống không cho sửa trực tiếp, chỉ tạo bản ghi mới kèm ghi chú đính chính (NFR9.1). \\
\textbf{Hậu điều kiện} & Bản ghi được lưu vĩnh viễn (append-only), sẵn sàng hiển thị trên trang truy xuất công khai sau khi sản phẩm được duyệt. \\
\end{longtable}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-B3 --- Hệ thống sinh mã QR truy xuất nguồn gốc}
\label{tab:uc-b3} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-B3: Hệ thống sinh mã QR truy xuất nguồn gốc}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Hệ thống (tự động, kích hoạt bởi UC-B4 khi quản trị viên kiểm duyệt duyệt sản phẩm) \\
\rowcolor{tblaltbg}
\textbf{Tiền điều kiện} & Sản phẩm/lô hàng đã qua kiểm duyệt và đạt điều kiện tối thiểu \\
\textbf{Luồng chính} &
1. Sinh mã định danh duy nhất theo nguyên lý GS1 (GTIN + batch/lot).\newline
2. Sinh mã QR liên kết tới trang truy xuất công khai.\newline
3. Trang truy xuất tổng hợp hồ sơ nhà cung cấp, chứng nhận đã duyệt, nhật ký canh tác, các mốc vận chuyển liên quan.\newline
4. Gắn mã QR vào trang sản phẩm, có thể in lên bao bì. \\
\rowcolor{tblaltbg}
\textbf{Luồng thay thế/ngoại lệ} & 1a. Lô đã có mã QR từ trước (do đính chính): không sinh mã mới, chỉ cập nhật nội dung trang truy xuất. \\
\textbf{Hậu điều kiện} & Mã QR khả dụng, quét được, dẫn tới trang truy xuất công khai đầy đủ và cập nhật. \\
\end{longtable}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-C4 --- Thanh toán đơn hàng bán lẻ}
\label{tab:uc-c4} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-C4: Thanh toán đơn hàng bán lẻ}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Khách lẻ (\texttt{buyer\_retail}) \\
\rowcolor{tblaltbg}
\textbf{Actor phụ} & Cổng thanh toán trung gian \\
\textbf{Tiền điều kiện} & Đơn hàng đã được tạo, đang chờ thanh toán \\
\rowcolor{tblaltbg}
\textbf{Luồng chính} &
1. Chọn phương thức thanh toán (COD/chuyển khoản-VietQR/ví điện tử).\newline
2. Nếu trực tuyến: hệ thống chuyển yêu cầu tới cổng thanh toán trung gian.\newline
3. Cổng thanh toán xử lý, trả kết quả.\newline
4. Hệ thống xác nhận đơn, tạm trừ tồn kho.\newline
5. Thông báo cho bộ phận đóng gói chuẩn bị hàng. \\
\textbf{Luồng thay thế/ngoại lệ} &
3a. Thanh toán thất bại: đơn giữ trạng thái chờ tối đa 15 phút rồi tự hủy, giải phóng tồn kho.\newline
1a. Chọn COD: bỏ qua bước 2--3, xác nhận ngay.\newline
4a. Nhiều đơn vượt tồn kho còn lại: ưu tiên đơn xác nhận thanh toán trước. \\
\rowcolor{tblaltbg}
\textbf{Hậu điều kiện} & Đơn chuyển trạng thái đã xác nhận (hoặc hủy nếu thất bại), tồn kho cập nhật đúng kết quả giao dịch. \\
\end{longtable}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-D4 --- Sinh hợp đồng điện tử từ RFQ đã thống nhất}
\label{tab:uc-d4} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-D4: Sinh hợp đồng điện tử từ RFQ đã thống nhất}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Hệ thống (tự động, sau khi Nhà cung cấp và Khách sỉ hoàn tất đàm phán) \\
\rowcolor{tblaltbg}
\textbf{Tiền điều kiện} & RFQ đã được nhà cung cấp xác nhận/điều chỉnh, hai bên thống nhất số lượng, đơn giá, lịch giao qua kênh nhắn tin nội bộ \\
\textbf{Luồng chính} &
1. Lấy thông tin đã thống nhất (sản phẩm, số lượng, đơn giá theo bậc, lịch giao, điều kiện thanh toán).\newline
2. Điền vào mẫu hợp đồng chuẩn, gồm điều khoản phạt vi phạm và thanh toán/công nợ.\newline
3. Gửi hợp đồng nháp cho cả hai bên xem trước khi ký (UC-D5). \\
\rowcolor{tblaltbg}
\textbf{Luồng thay thế/ngoại lệ} & 1a. Giá trị hợp đồng vượt ngưỡng quy định: đánh dấu thuộc diện áp dụng ký quỹ trung gian (FR7.3). \\
\textbf{Hậu điều kiện} & Hợp đồng điện tử ở trạng thái ``chờ ký'', lưu lịch sử phiên bản. \\
\end{longtable}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-D7 --- Xác nhận nhận hàng và thanh toán theo đợt}
\label{tab:uc-d7} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-D7: Xác nhận nhận hàng và thanh toán theo đợt}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Khách sỉ (\texttt{buyer\_wholesale}) \\
\rowcolor{tblaltbg}
\textbf{Actor phụ} & Cổng thanh toán trung gian \\
\textbf{Tiền điều kiện} & Nhà cung cấp đã giao hàng đợt tương ứng theo hợp đồng đã ký; hệ thống đã sinh hóa đơn cho đợt giao đó \\
\rowcolor{tblaltbg}
\textbf{Luồng chính} &
1. Kiểm tra và xác nhận đã nhận đủ hàng đúng cam kết của đợt giao.\newline
2. Thanh toán theo kỳ hạn công nợ đã thỏa thuận.\newline
3. Nếu hợp đồng có ký quỹ: hệ thống ra lệnh cho cổng thanh toán giải ngân cho nhà cung cấp ngay tại bước xác nhận này.\newline
4. Nhà cung cấp theo dõi doanh thu/lịch sử bán sỉ cập nhật. \\
\textbf{Luồng thay thế/ngoại lệ} &
1a. Không giao đủ số lượng/đúng lịch: áp dụng điều khoản phạt, ghi vào lịch sử uy tín.\newline
2a. Chậm thanh toán quá hạn: nhắc tự động, có thể tạm khóa quyền đặt RFQ mới.\newline
3a. Phát sinh tranh chấp về đợt giao đã ký quỹ: khoản ký quỹ giữ nguyên tại cổng thanh toán cho đến khi khiếu nại xử lý xong. \\
\rowcolor{tblaltbg}
\textbf{Hậu điều kiện} & Đợt giao chuyển trạng thái hoàn tất, công nợ cập nhật, tiền giải ngân cho nhà cung cấp (nếu có ký quỹ). \\
\end{longtable}
\end{center}

\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{L{2.6cm} L{11.5cm}}
\caption{Đặc tả use case UC-C7 --- Kiểm duyệt đánh giá nghi vấn}
\label{tab:uc-c7} \\
\toprule
\rowcolor{tblheadbg}
\multicolumn{2}{l}{\textbf{UC-C7: Kiểm duyệt đánh giá nghi vấn}} \\
\midrule
\endfirsthead
\bottomrule
\endfoot
\textbf{Actor chính} & Quản trị viên vận hành (\texttt{admin\_ops}) \\
\rowcolor{tblaltbg}
\textbf{Actor phụ} & Hệ thống (luật ngưỡng giám sát bất thường, Chương~\ref{chap:coso}) \\
\textbf{Tiền điều kiện} & Người mua đã gửi đánh giá sau khi xác nhận nhận hàng \\
\rowcolor{tblaltbg}
\textbf{Luồng chính} &
1. Kiểm duyệt tự động sơ bộ (từ khóa cấm, liên kết đáng ngờ).\newline
2. Áp dụng luật ngưỡng để tìm dấu hiệu giả mạo/spam/thao túng uy tín.\newline
3. Không nghi ngờ: công khai ngay.\newline
4. Bị nghi ngờ: chuyển quản trị viên xem xét thủ công.\newline
5. Quyết định công khai hoặc ẩn.\newline
6. Cập nhật điểm uy tín tổng hợp (không tính đánh giá bị ẩn). \\
\textbf{Luồng thay thế/ngoại lệ} &
5a. Xác nhận đánh giá giả/vi phạm: ẩn, thông báo lý do, không tính điểm uy tín.\newline
5b. Phát hiện nhà cung cấp thao túng đánh giá: chuyển quy trình xử lý vi phạm (mục~\ref{sec:quytrinhnghiepvu}, Quản trị hệ thống). \\
\rowcolor{tblaltbg}
\textbf{Hậu điều kiện} & Đánh giá ở trạng thái công khai hoặc ẩn dứt điểm; điểm uy tín cập nhật chính xác. \\
\end{longtable}
\end{center}


\section{Giải pháp công nghệ}
\label{sec:giaiphapcongnghe}

% TODO: nhóm xác nhận lại lựa chọn cuối cùng cho khớp với công nghệ thực sự dùng khi hiện thực (Chương 6), đặc biệt phần back-end nếu nhóm quen ngôn ngữ khác.

Mỗi hạng mục dưới đây được quyết định bằng cách nêu các phương án ứng viên, đối chiếu theo một bộ tiêu chí chung rồi chọn phương án phù hợp nhất. Bộ tiêu chí gồm năm điểm:
\begin{itemize}
    \item \textbf{Khả năng đáp ứng yêu cầu phi chức năng} đã đặt ra tại mục~\ref{sec:yeucauphichucnang}, đặc biệt là hiệu năng và tính toàn vẹn dữ liệu.
    \item \textbf{Mức phù hợp với đặc thù nông sản:} dữ liệu tổ chức theo lô, nhiều hình ảnh, tìm kiếm theo vùng trồng và mùa vụ.
    \item \textbf{Độ trưởng thành và hệ sinh thái:} tài liệu, thư viện sẵn có, cộng đồng hỗ trợ.
    \item \textbf{Chi phí:} ưu tiên phương án mã nguồn mở hoặc có gói miễn phí, phù hợp nguồn lực đồ án.
    \item \textbf{Chi phí học của nhóm:} thời gian thực hiện giới hạn nên việc phải học công nghệ hoàn toàn mới là một điểm trừ đáng kể.
\end{itemize}

Nguyên tắc xuyên suốt khi cân nhắc các phương án là \textbf{giảm tối đa số thành phần hạ tầng phải vận hành}. Mỗi thành phần thêm vào đều kéo theo chi phí cài đặt, giám sát và đồng bộ dữ liệu --- chi phí đó chỉ đáng trả khi không có cách nào khác. Bảng đối sánh chi tiết từng hạng mục được trình bày tại Phụ lục I; phần dưới đây chỉ nêu kết luận và lý do chính.

\subsection{Front-end}

\noindent\textbf{Phương án chọn: Next.js (nền React).} Các ứng viên được cân nhắc gồm React kết hợp Next.js, Vue kết hợp Nuxt, và Angular.

Yếu tố quyết định là trang truy xuất nguồn gốc: đây là trang mở công khai, không yêu cầu đăng nhập, được truy cập bằng cách quét mã QR in trên bao bì, và cần được công cụ tìm kiếm lập chỉ mục để người mua tra cứu được cả khi không vào nền tảng. Loại trang này đòi hỏi server-side rendering (SSR) --- điều mà Next.js hỗ trợ sẵn. Ngoài ra, việc dùng chung một framework cho cả giao diện mua hàng lẫn trang quản trị giúp giảm khối lượng công việc trùng lặp.

\subsection{Back-end}

\noindent\textbf{Framework --- phương án chọn: NestJS trên nền Node.js và TypeScript.} Các ứng viên gồm NestJS, Spring Boot, Django hoặc FastAPI, và Laravel. NestJS được chọn vì dùng chung ngôn ngữ TypeScript với phần giao diện, nhờ đó giảm một lần học và cho phép chia sẻ định nghĩa kiểu dữ liệu giữa hai phía; cấu trúc chia theo mô-đun cùng cơ chế guard kiểm soát quyền truy cập có sẵn cũng phù hợp trực tiếp với mô hình phân quyền nhiều vai trò của Farmery (mục~\ref{sec:doituongnguoidung}).

\noindent\textbf{Hệ quản trị cơ sở dữ liệu --- phương án chọn: PostgreSQL.} Các ứng viên gồm PostgreSQL, MySQL và MongoDB. Đây là lựa chọn then chốt nhất vì một mình PostgreSQL đáp ứng được ba nhu cầu mà nếu chọn khác sẽ phải dựng thêm nhiều thành phần riêng:
\begin{itemize}
    \item \textbf{Transaction có row-level locking} --- cần thiết để chống bán vượt tồn kho khi cùng một lô hàng được giao dịch song song ở hai luồng (NFR4.1). Đây là ràng buộc kỹ thuật khó nhất của hệ thống, và cơ sở dữ liệu hướng tài liệu như MongoDB xử lý kém hơn hẳn.
    \item \textbf{Dữ liệu không gian (PostGIS)} --- phục vụ quản lý vùng trồng và tính bán kính giao hàng, khớp trực tiếp với phạm vi không gian đã xác định tại mục~\ref{sec:phamvi}.
    \item \textbf{Kiểu dữ liệu JSON có đánh chỉ mục} --- phù hợp với nhật ký canh tác, vốn có tập trường khác nhau giữa từng loại cây trồng.
\end{itemize}

\noindent\textbf{Tìm kiếm và lọc nâng cao --- phương án chọn: full-text search sẵn có của PostgreSQL kèm đánh chỉ mục.} Ứng viên thay thế là Elasticsearch hoặc Meilisearch. Ở quy mô mục tiêu 100 người dùng đồng thời (NFR1.2), năng lực tìm kiếm sẵn có của cơ sở dữ liệu đã đủ đáp ứng yêu cầu lọc theo khoảng giá, vùng miền, loại chứng nhận và tình trạng còn hàng (FR4.5). Dựng thêm một search engine riêng sẽ phát sinh bài toán đồng bộ dữ liệu giữa hai nơi --- chi phí không tương xứng với lợi ích ở giai đoạn này. Ngưỡng cần chuyển đổi là khi thời gian truy vấn tìm kiếm vượt ngưỡng đặt ra tại NFR1.1.

\noindent\textbf{Lưu trữ tệp --- phương án chọn: object storage tương thích S3 (MinIO tự vận hành).} Hệ thống lưu lượng lớn ảnh sản phẩm, ảnh nhật ký canh tác và tệp chứng nhận; lưu trực tiếp trong cơ sở dữ liệu làm phình dung lượng và chậm sao lưu, còn lưu trên đĩa máy chủ ứng dụng thì cản trở việc nhân bản khi cần mở rộng (NFR2.1).

\noindent\textbf{Cache và hàng đợi --- phương án chọn: Redis.} Một thành phần phục vụ hai nhu cầu: cache các truy vấn đọc nhiều, và làm hàng đợi cho các tác vụ chạy nền như gửi thông báo, sinh mã QR và thu thập dữ liệu cho các mô-đun AI. Dựng riêng một message broker chuyên dụng là không cần thiết ở quy mô này.

\noindent\textbf{Dịch vụ bên thứ ba.} Cổng thanh toán chọn VNPay ở môi trường thử nghiệm, do tài liệu tiếng Việt đầy đủ nhất và hỗ trợ cơ chế tạm giữ tiền cần cho giao dịch số lượng lớn. Đơn vị vận chuyển chọn Giao Hàng Nhanh, do có API công khai và độ phủ tốt tại khu vực nông thôn --- yếu tố quan trọng vì đầu lấy hàng nằm tại vùng trồng.

\subsection{Thuật toán, công nghệ và mô hình AI}

Hai mô-đun AI trong phạm vi đồ án được tách thành một dịch vụ riêng viết bằng Python với khung FastAPI, do toàn bộ thư viện học máy cần dùng đều thuộc hệ sinh thái Python. Việc lựa chọn mô hình cho từng mô-đun bám theo ngưỡng chất lượng đã đặt tại Bảng~\ref{tab:nguongAI}; bảng đối sánh chi tiết các hướng tiếp cận nằm tại Phụ lục I.

\noindent\textbf{Gợi ý sản phẩm --- phương án chọn: mô hình hybrid giữa content-based filtering và collaborative filtering.} Các hướng được cân nhắc gồm collaborative filtering thuần túy, content-based filtering thuần túy, mô hình hybrid, và các mô hình mạng nơ-ron. Vấn đề quyết định là hiện tượng cold start đã nêu tại NC4: một nền tảng mới chưa có lịch sử tương tác, nên collaborative filtering thuần túy không hoạt động được ở giai đoạn đầu. Mô hình hybrid cho phép dựa vào đặc trưng sẵn có của nông sản --- mùa vụ, vùng trồng, loại chứng nhận, danh mục --- ngay từ khi chưa có dữ liệu hành vi, rồi tăng dần trọng số của thành phần collaborative filtering khi dữ liệu tích lũy.

\noindent\textbf{Dự báo giá --- phương án chọn: mô hình time series có thành phần mùa vụ (ARIMA, SARIMA, Prophet), đối chứng với mô hình gradient boosting.} Các hướng được cân nhắc gồm mô hình thống kê cổ điển (ARIMA, SARIMA), mô hình gradient boosting trên đặc trưng thời gian, và mạng nơ-ron hồi quy (RNN, LSTM). Các mô hình học sâu bị loại ở giai đoạn này vì chúng đòi hỏi lượng dữ liệu lớn hơn nhiều so với những gì có thể thu thập được; với chuỗi time series ngắn nhưng có tính mùa vụ rõ --- đúng đặc điểm giá nông sản --- mô hình thống kê thường cho kết quả tốt hơn mà lại dễ giải thích. Đây là một đánh đổi có ý thức: chọn mô hình đơn giản hơn nhưng kiểm chứng được, thay vì mô hình phức tạp hơn nhưng không đủ dữ liệu để đánh giá.

\noindent\textbf{Giám sát bất thường} không dùng mô hình học máy trong phạm vi đồ án mà hiện thực bằng các luật ngưỡng do quản trị viên cấu hình, với lý do đã phân tích tại Chương~\ref{chap:coso}.

\section{Thiết kế}
\label{sec:thietke}

\subsection{Thiết kế kiến trúc hệ thống}
\label{sec:kientruc}

% TODO: nhóm vẽ sơ đồ kiến trúc tổng thể minh họa mô tả dưới đây và chèn bằng \includegraphics.

Kiến trúc dưới đây là hệ quả trực tiếp của các lựa chọn công nghệ tại mục~\ref{sec:giaiphapcongnghe}, không phải một mô hình chọn sẵn từ trước.

\subsubsection*{Kiểu kiến trúc: modular monolith}

Hệ thống được xây dựng dưới dạng một modular monolith, tách riêng duy nhất dịch vụ AI. Ba phương án được cân nhắc là monolith phân lớp thuần túy, modular monolith, và kiến trúc microservices.

Kiến trúc microservices bị loại vì nó trả giá bằng độ phức tạp vận hành --- triển khai nhiều dịch vụ, theo dõi phân tán, xử lý giao dịch trải trên nhiều dịch vụ --- và cái giá đó chỉ đáng trả khi có nhiều đội phát triển làm việc song song trên cùng hệ thống, điều không đúng với một đồ án. Quan trọng hơn, nghiệp vụ khó nhất của Farmery là chống bán vượt tồn kho khi một lô hàng được giao dịch đồng thời ở hai luồng; nghiệp vụ này dựa vào transaction có row-level locking trong một cơ sở dữ liệu duy nhất, mà tách dịch vụ sẽ buộc phải thay bằng saga và compensating transaction phức tạp hơn nhiều.

Dịch vụ AI là ngoại lệ duy nhất được tách ngay từ đầu, vì nó viết bằng ngôn ngữ khác và có đặc tính tài nguyên khác hẳn phần lõi nghiệp vụ.

\subsubsection*{Ba ràng buộc để về sau vẫn tách được}

Chọn khối đơn không có nghĩa là đóng cửa khả năng tách về sau, nhưng khả năng đó chỉ tồn tại nếu được giữ gìn ngay từ khi viết mã. Ba ràng buộc dưới đây được đặt ra như cam kết thiết kế:

\begin{enumerate}
    \item[\textbf{RB1.}] \textbf{Ranh giới mô-đun chia theo miền nghiệp vụ, không theo tầng kỹ thuật.} Mã nguồn tổ chức thành các mô-đun như \emph{sản phẩm}, \emph{tồn kho}, \emph{bán sỉ}, \emph{thu mua}; mỗi mô-đun tự chứa đủ các tầng của nó, thay vì gom tất cả bộ điều khiển vào một thư mục và tất cả truy vấn dữ liệu vào một thư mục khác.
    \item[\textbf{RB2.}] \textbf{Mô-đun gọi nhau qua giao diện công khai.} Một mô-đun cần dữ liệu của mô-đun khác thì gọi dịch vụ do mô-đun đó công bố, không truy vấn trực tiếp vào bảng dữ liệu của nó.
    \item[\textbf{RB3.}] \textbf{Không kết nối bảng xuyên mô-đun ở tầng truy vấn.} Mỗi mô-đun sở hữu tập bảng riêng, tham chiếu sang mô-đun khác bằng định danh. Đây là ràng buộc quan trọng nhất: thứ khóa chặt một monolith không phải là mã nguồn mà là dữ liệu --- tách mã nguồn chỉ tốn vài ngày, còn gỡ một câu truy vấn kết nối năm bảng thuộc ba miền khác nhau đòi hỏi viết lại cả nghiệp vụ.
\end{enumerate}

Biện pháp bảo đảm ba ràng buộc trên với chi phí thấp nhất là cấp cho \textbf{mỗi mô-đun một schema riêng trong cùng một cơ sở dữ liệu}. Hệ thống vẫn chạy trên một cơ sở dữ liệu duy nhất nên giao dịch vẫn hoạt động bình thường, nhưng đường biên dữ liệu hiện ra rõ ràng và mọi vi phạm ràng buộc RB3 đều lộ ra ngay khi đọc mã.

\subsubsection*{Các lớp và thành phần}

Hệ thống gồm các lớp sau, mỗi lớp ghi rõ công nghệ đã chọn:
\begin{itemize}
    \item \textbf{Lớp giao diện} --- Next.js, phục vụ ba loại giao diện: trang mua hàng, trang quản lý của nhà cung cấp, và trang quản trị nội bộ.
    \item \textbf{Lớp ứng dụng} --- NestJS, tiếp nhận yêu cầu, kiểm tra quyền truy cập theo vai trò, điều phối giữa các mô-đun nghiệp vụ.
    \item \textbf{Lớp mô-đun nghiệp vụ} --- các mô-đun chia theo miền như mô tả ở mục~\ref{sec:luongvanhanh}, mỗi mô-đun một schema dữ liệu riêng.
    \item \textbf{Lớp dữ liệu} --- PostgreSQL cho dữ liệu nghiệp vụ, Redis cho cache và hàng đợi, object storage cho ảnh và tệp chứng nhận.
    \item \textbf{Dịch vụ AI} --- tách riêng, viết bằng Python, nhận dữ liệu qua event queue và trả kết quả qua API nội bộ.
    \item \textbf{Lớp tích hợp bên ngoài} --- cổng thanh toán và đơn vị vận chuyển, gọi qua API của bên thứ ba.
\end{itemize}

\subsubsection*{Các quyết định kiến trúc đáng chú ý}

\begin{itemize}
    \item \textbf{Tách dịch vụ AI khỏi lõi nghiệp vụ.} Ngoài lý do khác ngôn ngữ, việc tách còn bảo đảm sự cố ở mô-đun AI không làm gián đoạn giao dịch: khi dịch vụ gợi ý không phản hồi, trang chủ lùi về danh sách sản phẩm bán chạy thay vì báo lỗi.
    \item \textbf{Dùng event queue cho vòng phản hồi AI.} Các sự kiện như lượt xem, lượt mua, đánh giá được phát vào hàng đợi thay vì ghi đồng bộ, nhờ đó không làm chậm luồng giao dịch chính. Thiết kế này cũng khiến việc chuyển sang một message broker chuyên dụng về sau không phải sửa phần mã phát sự kiện.
    \item \textbf{Chống bán vượt tồn kho.} Khi một đơn hàng được tạo ở bất kỳ luồng nào, hệ thống row-level lock trên dòng tồn kho của lô tương ứng trong phạm vi một giao dịch, kiểm tra số lượng còn lại rồi mới trừ. Đây là lý do cốt lõi khiến tồn kho và đơn hàng phải nằm chung một cơ sở dữ liệu.
    \item \textbf{Tách đường phục vụ trang truy xuất công khai.} Trang truy xuất mã QR không yêu cầu đăng nhập và có thể bị truy cập dồn dập khi một lô hàng được nhiều người quét cùng lúc. Trang này chỉ đọc dữ liệu, được cache tích cực và tách khỏi vùng cần xác thực --- đây cũng là thành phần nên tách ra trước tiên nếu về sau hệ thống cần chia nhỏ.
\end{itemize}


\subsection{Thiết kế sitemap}
% TODO: sơ đồ các trang/màn hình theo từng nhóm người dùng.

\subsection{Thiết kế lược đồ tuần tự}
% TODO: sequence diagram cho các luồng chính: đặt hàng B2C, RFQ -- hợp đồng -- ký quỹ B2B, đăng và kiểm duyệt sản phẩm, gửi và kiểm duyệt đánh giá.

\subsection{Thiết kế lược đồ lớp}
% TODO: class diagram cho các lớp miền chính, bám theo các nhóm thực thể tại mục 5.3.5.

\subsection{Thiết kế cơ sở dữ liệu}
\label{sec:csdl}

\subsubsection*{Đặc tả cơ sở dữ liệu}
\label{subsec:dacta-csdl}

Cơ sở dữ liệu được tổ chức theo tám nhóm thực thể tương ứng với các nhóm chức
năng FR1--FR12 tại mục~\ref{sec:yeucauchucnang}, đồng thời chính là các mô-đun
nghiệp vụ đã nêu tại mục~\ref{sec:kientruc} --- mỗi nhóm sở hữu một schema
riêng theo ràng buộc RB3:

\begin{enumerate}
    \item \textbf{Người dùng và phân quyền} (FR1) --- tài khoản, địa chỉ nhận hàng, hồ sơ doanh nghiệp.
    \item \textbf{Gian hàng, chứng nhận, sản phẩm và truy xuất nguồn gốc} (FR2--FR4) --- gian hàng, chứng nhận, danh mục, sản phẩm, lô hàng, nhật ký canh tác, mã QR truy xuất.
    \item \textbf{Giao dịch bán lẻ} (FR5) --- giỏ hàng, đơn hàng, dòng hàng, mã giảm giá, thanh toán.
    \item \textbf{Giao dịch bán sỉ} (FR6) --- bảng giá bậc, yêu cầu báo giá, lịch sử thương lượng, hợp đồng, đợt giao hàng.
    \item \textbf{Vận chuyển và khiếu nại} (FR8) --- vận đơn, mốc hành trình, khiếu nại và phán quyết.
    \item \textbf{Tương tác và thông báo} (FR9, FR11) --- đánh giá, phản hồi của nhà cung cấp, tin nhắn, thông báo.
    \item \textbf{Hỗ trợ AI} (FR12) --- sự kiện hành vi, kết quả gợi ý và kết quả dự báo.
    \item \textbf{Quản trị, vi phạm và lưu vết} (FR10, NFR9.1) --- hồ sơ vi phạm, cảnh báo do luật ngưỡng sinh ra, nhật ký thao tác quản trị.
\end{enumerate}

Đặc tả đầy đủ từng trường của tất cả các thực thể --- gồm kiểu dữ liệu, ràng
buộc và mã yêu cầu liên quan --- được trình bày tại Phụ lục K. Mục này chỉ
giữ lại phần tổ chức tổng thể và sơ đồ quan hệ thực thể dưới đây.

\subsubsection*{Sơ đồ quan hệ thực thể (ERD)}
\label{subsec:erd}

Dựa trên các thực thể và mối quan hệ đã đặc tả, mô hình quan hệ thực thể (ERD)
tổng quát của hệ thống Farmery được thể hiện tại Hình~\ref{fig:erd}. Sơ đồ tổng
hợp mối liên kết giữa 8 nhóm thực thể nghiệp vụ: tài khoản và phân quyền người
dùng, quản lý gian hàng và truy xuất nguồn gốc, chu trình giao dịch bán lẻ B2C
và bán sỉ B2B, vận chuyển, khiếu nại, tương tác người dùng, các bảng phục vụ
tính năng AI, cùng nhóm bảng quản trị/lưu vết vi phạm.

\begin{figure}[H]
\centering
% \includegraphics[width=\textwidth]{image/erd.png}
\caption{Sơ đồ quan hệ thực thể (ERD) hệ thống Farmery}
\label{fig:erd}
\end{figure}
% TODO: nhóm bổ sung file ảnh sơ đồ ERD vào image/erd.png và bỏ chú thích dòng lệnh \includegraphics ở trên. Lưu ý vẽ theo bản đặc tả hiện hành tại mục~\ref{subsec:dacta-csdl} (8 nhóm thực thể, đã gồm Nhóm 8 quản trị/lưu vết, trường điểm uy tín của Shop và các khóa ngoại nullable của Payment cho luồng B2B).


\subsection{Thiết kế mô-đun AI}
\label{sec:thietkeAI}

\subsubsection*{Luồng dữ liệu tổng thể}

% TODO: nhóm bổ sung sơ đồ kiến trúc cụ thể (\includegraphics) minh họa luồng mô tả dưới đây, và chốt công nghệ/mô hình dùng cho từng bước khi hiện thực (mục~\ref{sec:hienthucAI}).
Cả hai mô-đun AI (Chương~\ref{chap:coso}) đều đi theo một luồng dữ liệu chung gồm 5 bước:
\begin{enumerate}
    \item \textbf{Thu thập}: ghi nhận sự kiện từ các quy trình nghiệp vụ (lượt xem/mua sản phẩm, đơn hàng, đánh giá, hành vi hủy đơn) vào kho dữ liệu.
    \item \textbf{Tiền xử lý}: làm sạch, chuẩn hóa và trích xuất đặc trưng (feature engineering) theo từng bài toán.
    \item \textbf{Huấn luyện và suy luận}: huấn luyện định kỳ theo lô (batch, ví dụ mỗi tuần) cho cả hai mô-đun; riêng phần gợi ý có thêm bước suy luận trực tuyến (online inference) để phản hồi ngay theo phiên duyệt của người dùng.
    \item \textbf{Phục vụ kết quả}: trả kết quả qua API nội bộ để các mô-đun nghiệp vụ khác sử dụng (trang chủ, trang quản trị).
    \item \textbf{Vòng phản hồi}: ghi nhận phản ứng của người dùng với kết quả AI (có nhấp vào gợi ý không, đánh giá đúng/sai của quản trị viên với cảnh báo gian lận) để làm dữ liệu huấn luyện lại, cải thiện mô hình theo thời gian.
\end{enumerate}

Danh sách dưới đây tóm tắt đầu vào/đầu ra riêng của từng mô-đun trong luồng chung này:
\begin{itemize}
    \item \textbf{Gợi ý sản phẩm}: đầu vào là lịch sử tương tác/mua hàng của người dùng và đặc trưng sản phẩm (danh mục, vùng trồng, mùa vụ); đầu ra là danh sách sản phẩm được xếp hạng theo mức độ phù hợp, hiển thị tại trang chủ và trang sản phẩm liên quan.
    \item \textbf{Dự báo nhu cầu/giá}: đầu vào là dữ liệu lịch sử đơn hàng, sản lượng theo mùa vụ và vùng trồng; đầu ra là ước lượng nhu cầu/giá cho giai đoạn tới, hiển thị cho nhà cung cấp như gợi ý kế hoạch sản xuất và cho quản trị viên như dữ liệu cảnh báo dư cung/khan hiếm.
\end{itemize}

\subsubsection*{Ngưỡng chất lượng mục tiêu}
\label{subsec:nguongAI}

Vì cả hai mô-đun AI đều chưa có dữ liệu vận hành thực tế ở thời điểm thiết kế, ngưỡng chất lượng mục tiêu được đặt ra ngay từ bước thiết kế --- thay vì chỉ mô tả định tính --- để làm căn cứ đối chiếu khi đánh giá kết quả thực nghiệm tại Chương~\ref{chap:danhgia}. Mỗi mô-đun được đo bằng chỉ số phù hợp với bài toán của nó: mức tăng click-through rate (CTR) so với danh sách baseline cho gợi ý sản phẩm, và MAPE cho dự báo giá. Bảng~\ref{tab:nguongAI} tại Phụ lục L trình bày đầy đủ từng ngưỡng kèm lý do chọn.


\subsection{Thiết kế API}
% TODO: danh sách endpoint chính theo nhóm FR (phương thức, đường dẫn, quyền truy cập RBAC, dữ liệu vào/ra); document hóa theo OpenAPI/Swagger (NFR5.2).

\subsection{Thiết kế UI/UX}
% TODO: wireframe/mockup các màn hình chính; lưu ý nguyên tắc tối giản cho nhóm nông dân/HTX (NFR6).

\subsection{Thiết kế test case}
\label{sec:kichbankiemthu}
% TODO: bảng test case theo từng nhóm FR (mã test, mô tả, dữ liệu vào, kết quả mong đợi); test case phi chức năng bám theo Bảng~\ref{tab:nguongNFR}.

\subsection{Các chính sách đối với xử lý của hệ thống và các bên thứ ba}
\label{subsec:chinhsachbenthuba}

\begin{itemize}
    \item \textbf{Cổng thanh toán trung gian:} mọi giao dịch thanh toán và ký quỹ escrow đi qua cổng thanh toán được cấp phép; hệ thống không lưu dữ liệu thẻ (NFR3.4), chỉ lưu mã giao dịch để đối soát và ra lệnh giải ngân khi đủ điều kiện (FR7.3).
    \item \textbf{Đơn vị vận chuyển thứ ba (3PL):} tích hợp qua REST API, mỗi lần gọi có thời gian chờ tối đa 5 giây và cơ chế thử lại khi lỗi tạm thời (NFR7.1); sự cố giao hàng được theo dõi trên bảng điều khiển giám sát (FR10.5).
    \item \textbf{Đăng nhập qua bên thứ ba:} hỗ trợ OAuth 2.0; tài khoản đăng nhập qua mạng xã hội vẫn phải xác thực OTP cho các thao tác nhạy cảm (FR1.1, FR1.2).
    \item \textbf{Xác minh chứng nhận:} trong phạm vi đồ án, việc xác minh được thực hiện thủ công; khi có kết nối với cơ quan cấp chứng nhận sẽ chuyển sang đối chiếu tự động.
    \item \textbf{Xử lý nội bộ:} giới hạn tần suất thao tác (khóa 30 phút sau 5 lần sai), khóa tồn kho khi đặt hàng để chống bán vượt, lưu vết toàn bộ thao tác quản trị (NFR3.5, NFR1.2).
\end{itemize}
% TODO: bổ sung chính sách cụ thể theo điều khoản của từng đối tác sau khi chốt nhà cung cấp dịch vụ.

\section{Kết chương}

Chương này đã mô hình hóa tám quy trình nghiệp vụ bằng lưu đồ, trình bày thiết kế cơ sở dữ liệu tám nhóm thực thể, thiết kế hai mô-đun AI kèm ngưỡng chất lượng mục tiêu và các chính sách xử lý với bên thứ ba.
% TODO: cập nhật kết chương sau khi hoàn thiện use case, giải pháp công nghệ, kiến trúc, sitemap, lược đồ tuần tự/lớp, API, UI/UX và test case.
